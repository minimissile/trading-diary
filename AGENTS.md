# 项目协作约束

本文件适用于整个 trading-diary 项目。以下规则来自用户明确要求，应在每轮工作中执行；用户当轮明确要求及更高优先级指令优先。

## 默认速度优先

用户接受小改动交付后根据反馈再次修正，不接受为小改动进行长时间分析、无关搜索和过度验证。默认以最短必要路径交付，不以追求一次覆盖所有情况为目标。

### 小改动的硬性执行规则

适用范围：文案、颜色、尺寸、间距、对齐、简单 CSS 动画及其他局部、可逆、低风险修改。

1. 默认流程只有：一轮局部定位 → 修改 → 简短交付。定位只读取直接相关文件；只有现有信息确实无法定位时才追加定向读取。
2. 不主动启动 Electron、开发服务器、浏览器或截图流程；不运行全量测试、全项目 lint、全项目类型检查或构建。
3. 不为这类改动新写测试、临时验证脚本或测试页面，不调用子代理，不搜索外部资料，除非用户明确要求或更高优先级指令要求。
4. 修改后至多进行一轮轻量静态检查，限本次修改的文件或差异；不为完成检查而安装依赖、调整环境或调试验证工具。
5. 验证工具失败时，停止折腾工具，交付已完成的修改并说明未验证；若失败直接暴露修改错误，则修复该错误。
6. 不顺带重构、更新无关文档、格式化无关代码或扩大需求范围。不覆盖工作区中已有的其他修改。
7. 最终回复简短说明改动。仅在实际运行验证后声称“已验证”；否则不得将“已修改”描述成“已验证”。

### 允许增加检查的情况

- 用户明确要求测试、全面排查或深入审查。
- 修改涉及金额计算、交易数据写入、数据库迁移、权限、安全或不可逆操作。
- 已有证据表明存在跨模块依赖、回归或复杂问题，局部修改无法可靠完成任务。
- 更高优先级指令明确要求额外检查。

增加检查前，用一句话说明本次具体原因和必要检查范围，不因假设风险自动升级流程。检查须与实际风险相匹配，不扩大到无关模块。

## 每轮执行前自检

- 这是不是局部、可逆的小改动？若是，严格走上述短流程。
- 下一次读取或工具调用是否直接服务于用户本次请求？若不是，省略。
- 已经可以交付了吗？若可以，立即交付，不继续补充可选验证。
